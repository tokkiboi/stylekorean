/* After deploying google-apps-script/Code.gs as a domain-restricted web app,
   paste its /exec URL below. Keep the endpoint restricted to @stylekoreanus.com. */
window.STYLEKOREAN_CONFIG = {
  completeEndpoint: ""
};
