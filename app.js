const ID="1M-vZ24Yw4ZN7R7b_473cVn8kny8DznTakSsD3VQsCzc", done=new Set(["Delivered","Received","Completed"]), finished=new Set(["Delivered","Received","Completed","Cancelled"]); let inbound=[],outbound=[],costSummary={ytd:0,mtd:0,finished:0};
const AUTO_REFRESH_INTERVAL=5*60*1000;
let autoRefreshEnabled=true,autoRefreshTimer=null,countdownTimer=null,nextRefreshAt=null,taskRows=[],importScheduleRows=[];
const $=id=>document.getElementById(id), esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const sourceKey=v=>String(v).toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");
function parse(text){const a=text.indexOf("{"),b=text.lastIndexOf("}");if(a<0||b<0)throw Error("Google Sheets did not return data");return JSON.parse(text.slice(a,b+1)).table}
function objects(table){const headers=table.cols.map((c,i)=>(c.label||`COL_${i}`).toUpperCase().replace(/\s+/g," "));return table.rows.map(r=>Object.fromEntries(headers.map((h,i)=>{const c=r.c[i];return[h,c?(c.f??c.v??"").toString().trim():""]})))}
async function sheet(name,range,spreadsheetId=ID){const u=new URL(`https://docs.google.com/spreadsheets/d/${spreadsheetId}/gviz/tq`);u.searchParams.set("tqx","out:json");u.searchParams.set("headers","1");u.searchParams.set("sheet",name);if(range)u.searchParams.set("range",range);const r=await fetch(u,{cache:"no-store"});if(!r.ok)throw Error(`${name}: ${r.status}`);return parse(await r.text())}
const get=(r,...n)=>{for(const k of n){const v=r[k.toUpperCase()];if(v)return v}return""}, useful=r=>Object.values(r).some(Boolean);
const parcelCarriers=/^(UPS|USPS|DHL|AMAZON|FEDEX)$/i;
const planningLabels=/^(URGENT|SCHEDULED|NEED SCHEDULING|COMPLETED)$/i;
function looksLikeParcelTracking(raw){const n=String(raw||"").replace(/\s/g,"").toUpperCase();return /^1Z[0-9A-Z]{16}$/.test(n)||/^TBA[0-9A-Z]+$/.test(n)||/^(94|93|92|95)\d{18,22}$/.test(n)||/^[A-Z]{2}\d{9}US$/.test(n)||/^\d{10}$/.test(n)||/^(?:\d{12}|\d{15}|\d{20}|\d{22})$/.test(n)}
function containerNumbers(v){return String(v||"").split(/[,\n]+/).map(p=>p.trim().replace(/\s/g,"").toUpperCase()).filter(p=>/^[A-Z]{4}\d{7}$/.test(p))}
function isInboundShipment(r){const shipment=get(r,"SHIPMENT"),invoice=get(r,"INVOICE"),container=get(r,"CONTAINER","CONTAINER RAW (SYSTEM)"),mbl=get(r,"MBL").replace(/\s/g,""),rowText=Object.values(r).join(" "),hasInvoice=/\bIN\d{4,}\b/i.test(invoice),hasCleanContainer=containerNumbers(container).length>0,hasAirWaybill=/^\d{3}-?\d{8}$/.test(mbl),isPlanningRow=planningLabels.test(shipment.trim())||(!hasInvoice&&!hasCleanContainer&&!hasAirWaybill&&/-\s*2026\b/.test(rowText)),hasParcelTracking=Object.values(r).some(looksLikeParcelTracking),hasShipmentIdentifier=!/^NEW$/i.test(shipment.trim())&&(hasInvoice||hasCleanContainer||hasAirWaybill),sectionLabel=Object.values(r).find(v=>parcelCarriers.test(String(v).trim()));return useful(r)&&hasShipmentIdentifier&&!hasParcelTracking&&!sectionLabel&&!isPlanningRow}
function status(v){v=v.toLowerCase();if(/cancel/.test(v))return"Cancelled";if(/\bshipped\b|complete|closed/.test(v))return"Completed";if(/deliver/.test(v))return"Delivered";if(/receive/.test(v))return"Received";if(/shipping|transit|progress/.test(v))return"Shipping";return"Scheduled"}
function money(v){const m=String(v).replace(/,/g,"").match(/-?\d+(?:\.\d+)?/g);return m?m.reduce((s,n)=>s+Number(n),0):0}
function outboundCost(r){for(const n of ["INVOICE AMOUNT","RATE QUOTE AMOUNT","RATE QUOTE","QUOTE AMOUNT","QUOTE","RATE"]){const v=get(r,n);if(v)return money(v)}const invoice=get(r,"INVOICE");return invoice.includes("$")?money(invoice):0}
function formatDate(v){const m=String(v||"").match(/(\d{1,2})\/(\d{1,2})(?:\/(\d{2,4}))?/);if(!m)return String(v||"");const mo=+m[1];let y=m[3]?+m[3]:mo>=9?2025:2026;if(y<100)y+=2000;return`${String(mo).padStart(2,"0")}/${String(+m[2]).padStart(2,"0")}/${String(y).slice(-2)}`}
function lastDate(v){const matches=[...String(v||"").matchAll(/(\d{1,2})\/(\d{1,2})(?:\/(\d{2,4}))?/g)];return matches.length?formatDate(matches.at(-1)[0]):""}
function beforeJune2026(v){const m=String(v||"").match(/(\d{1,2})\/(\d{1,2})(?:\/(\d{2,4}))?/);if(!m)return false;const mo=+m[1];let y=m[3]?+m[3]:mo>=9?2025:2026;if(y<100)y+=2000;return new Date(y,mo-1,+m[2])<new Date(2026,5,1)}
function freight(r){return{qty:get(r,"QTY","QUANTITY","TOTAL CARTONS","PLT"),length:get(r,"LENGTH (IN)","LENGTH","L"),width:get(r,"WIDTH (IN)","WIDTH","W"),height:get(r,"HEIGHT (IN)","HEIGHT","H"),freightClass:get(r,"FREIGHT CLASS","CLASS"),subClass:get(r,"SUB CLASS","SUBCLASS")}}
const oceanScac={SMLM:"SM Line",HDMU:"HMM",MAEU:"Maersk",MSCU:"MSC",CMDU:"CMA CGM",COSU:"COSCO",OOLU:"OOCL",ONEY:"ONE",EGLV:"Evergreen",YMLU:"Yang Ming",ZIMU:"ZIM",HLCU:"Hapag-Lloyd",KORP:"KMTC"};
function destinationPort(r){return get(r,"DESTINATION PORT","PORT OF DISCHARGE","POD","DISCHARGE PORT","DESTINATION")||"Los Angeles / Long Beach, CA"}
function officialOceanTracking(carrier,container,mbl){const n=encodeURIComponent(String(container||mbl||"").replace(/\s/g,"").toUpperCase());if(!n)return"";if(carrier==="SM Line")return`https://esvc.smlines.com/smline/CUP_HOM_3301GS.do?_search=false&f_cmd=121&page=1&rows=10000&search_name=${n}&search_type=C&sidx=&sord=asc`;if(carrier==="HMM")return`https://www.hmm21.com/e-service/general/trackNTrace/TrackNTrace.do?searchType=CNTR&searchNo=${n}`;if(carrier==="Maersk")return`https://www.maersk.com/tracking/${n}`;if(carrier==="MSC")return`https://www.msc.com/en/track-a-shipment?trackingNumber=${n}`;if(carrier==="CMA CGM")return`https://www.cma-cgm.com/ebusiness/tracking/search?SearchBy=Container&Reference=${n}`;if(carrier==="Hapag-Lloyd")return`https://www.hapag-lloyd.com/en/online-business/track/track-by-container-solution.html?container=${n}`;if(carrier==="ONE")return`https://ecomm.one-line.com/one-ecom/manage-shipment/cargo-tracking?trakNoParam=${n}`;if(carrier==="KMTC")return"https://www.ekmtc.com/";if(carrier==="COSCO")return"https://elines.coscoshipping.com/ebusiness/cargoTracking";if(carrier==="OOCL")return"https://www.oocl.com/eng/ourservices/eservices/cargotracking/Pages/cargotracking.aspx";if(carrier==="Evergreen")return"https://ct.shipmentlink.com/servlet/TDB1_CargoTracking.do";if(carrier==="Yang Ming")return"https://www.yangming.com/e-service/track_trace/track_trace_cargo_tracking.aspx";if(carrier==="ZIM")return"https://www.zim.com/tools/track-a-shipment";return`https://www.track-trace.com/container?number=${n}`}
function tracking(container,mbl,vessel,destination){const n=String(container||"").replace(/\s/g,"").toUpperCase(),cleanMbl=String(mbl||"").replace(/[^A-Z0-9]/gi,"").toUpperCase(),scac=(cleanMbl.match(/^[A-Z]{4}/)||[""])[0];let carrier=oceanScac[scac]||"";if(!carrier){const v=String(vessel||"").toUpperCase();if(/\bHMM\b/.test(v))carrier="HMM";else if(/\bSM\s|SM LINE/.test(v))carrier="SM Line";else if(/MAERSK|HANSA EUROPE/.test(v))carrier="Maersk";else if(/KMTC/.test(v))carrier="KMTC";else if(/^(SMCU)/.test(n))carrier="SM Line";else if(/^(HMMU|HDMU)/.test(n))carrier="HMM";else if(/^(MSCU|MEDU)/.test(n))carrier="MSC";else if(/^(MAEU|MRKU|MSKU|MRSU)/.test(n))carrier="Maersk"}if(!carrier)carrier="Carrier tracking";return[carrier,officialOceanTracking(carrier,n,cleanMbl),scac,scac?`SCAC ${scac} · destination port: ${destination}`:`Carrier inferred from vessel/container · destination port: ${destination}`]}
function parcelTracking(carrier,raw){const n=encodeURIComponent(raw.replace(/\s/g,""));if(carrier==="UPS")return`https://www.ups.com/track?loc=en_US&tracknum=${n}`;if(carrier==="USPS")return`https://tools.usps.com/go/TrackConfirmAction?tLabels=${n}`;if(carrier==="DHL")return`https://www.dhl.com/us-en/home/tracking.html?tracking-id=${n}`;if(carrier==="FedEx")return`https://www.fedex.com/fedextrack/?trknbr=${n}`;return""}
function parcelCarrier(sectionCarrier,tracking){const n=tracking.replace(/\s/g,"").toUpperCase();if(/^1Z[0-9A-Z]{16}$/.test(n))return"UPS";if(/^TBA[0-9A-Z]+$/.test(n))return"Amazon";if(/^(94|93|92|95)\d{18,22}$/.test(n)||/^[A-Z]{2}\d{9}US$/.test(n))return"USPS";if(/^\d{10}$/.test(n))return"DHL";if(/^(?:\d{12}|\d{15}|\d{20}|\d{22})$/.test(n))return/^DHL$/i.test(sectionCarrier)?"DHL":"FedEx";if(/^FEDEX$/i.test(sectionCarrier))return"FedEx";if(/^DHL$/i.test(sectionCarrier))return"DHL";if(/^USPS$/i.test(sectionCarrier))return"USPS";if(/^AMAZON$/i.test(sectionCarrier))return"Amazon";if(/^UPS$/i.test(sectionCarrier))return"UPS";return sectionCarrier}
function parcelCarrierNote(sectionCarrier,carrier,tracking){const n=tracking.replace(/\s/g,"").toUpperCase();if(/^1Z[0-9A-Z]{16}$/.test(n))return"Carrier inferred from UPS 1Z tracking #";if(/^TBA[0-9A-Z]+$/.test(n))return"Carrier inferred from Amazon TBA tracking #";if(/^(94|93|92|95)\d{18,22}$/.test(n)||/^[A-Z]{2}\d{9}US$/.test(n))return"Carrier inferred from USPS tracking #";if(/^\d{10}$/.test(n))return"Carrier inferred from DHL 10-digit tracking #";if(/^(?:\d{12}|\d{15}|\d{20}|\d{22})$/.test(n))return/^DHL$/i.test(carrier)?"Carrier inferred from DHL numeric tracking #":"Carrier inferred from FedEx numeric tracking #";return/^FEDEX$/i.test(sectionCarrier)?"Carrier from FedEx section":`Carrier from ${carrier||sectionCarrier} section`}
function noteTrace(detail){let st=status(detail);if(/label created|not shipped/i.test(detail))st="Scheduled";else if(/pending|clearance|customs|waiting|transit/i.test(detail))st="Shipping";return{eta:lastDate(detail),status:st,summary:detail.trim()?`Imports note: ${detail.trim().replace(/\s+/g," ")}`:"No carrier tracking note yet",source:"Imports note"}}
function parcelInbound(table,startId){let carrier="";const result=[],value=(row,i)=>{const c=row.c?.[i];return c?(c.f??c.v??"").toString().trim():""};for(const row of table.rows){const section=value(row,0);if(parcelCarriers.test(section))carrier=/^fedex$/i.test(section)?"FedEx":section.toUpperCase();const number=value(row,1).replace(/^TRACKING#$/i,"").trim();if(!number||/^TRACK/i.test(number))continue;const actualCarrier=parcelCarrier(carrier,number);if(!actualCarrier)continue;const invoice=value(row,2),detail=value(row,4),trace=noteTrace(detail),carrierNote=parcelCarrierNote(carrier,actualCarrier,number);result.push({id:startId+result.length,mode:"Small Parcel",eta:trace.eta,shipmentNo:number,mbl:"",hbl:invoice,container:number,carrier:actualCarrier,trackingUrl:parcelTracking(actualCarrier,number),origin:value(row,3)||"Imports",destination:"StyleKorean warehouse",status:trace.status,trackingSummary:`${carrierNote} · ${trace.summary} · Live carrier API available on internal site when configured`,trackingSource:trace.source})}return result}
function out(source,r,map){const mapped=map(r);outbound.push({id:outbound.length+1,source,...freight(r),...mapped,shipDate:formatDate(mapped.shipDate),rate:outboundCost(r),cancelled:/cancel/i.test(Object.values(r).join(" ")),status:mapped.status==="Cancelled"?"Cancelled":beforeJune2026(mapped.shipDate)?"Completed":mapped.status})}
async function loadBase(){ $("sync").textContent="Importing outbound and inbound sources…";try{const sources=[["IMPORTS","A1:AD340"],["TRANSFERS","A1:N974"],["ULTA","A1:N1012"],["IHERB","A1:M967"],["B2B/E-COM TRUCKING","A1:R853"],["WH Trucking Request","A2:U1551"],["NATIONAL ORDER PROGRESS","A1:U2503"],["OUTBOUND WEBSITE EXCLUSIONS","A1:C500"]],results=await Promise.allSettled(sources.map(args=>sheet(...args))),tables=results.map(result=>result.status==="fulfilled"?result.value:{cols:[],rows:[]}),unavailable=results.flatMap((result,i)=>result.status==="rejected"?[sources[i][0]]:[]);const [im,tr,ul,ih,b2,whRows,national,exclusionRows]=tables.map(objects),wh=whRows.slice(1),siteExclusions=new Set(exclusionRows.filter(useful).map(r=>`${get(r,"SOURCE")}|${get(r,"KEY")}`.trim().toUpperCase()));
inbound=im.filter(isInboundShipment).map((r,i)=>{const containerRaw=get(r,"CONTAINER","CONTAINER RAW (SYSTEM)"),container=containerNumbers(containerRaw)[0]||containerRaw.split(/[,\n]/)[0],mbl=get(r,"MBL"),destination=destinationPort(r),[carrier,trackingUrl,carrierScac,trackingBasis]=tracking(container,mbl,get(r,"VSL"),destination);return{id:i+1,mode:container||!/^\d{3}-?\d+/.test(mbl)?"Ocean":"Air",eta:formatDate(get(r,"ETA")),shipmentNo:get(r,"SHIPMENT"),mbl,hbl:get(r,"HBL"),container,carrier:carrierScac?`${carrier} · ${carrierScac}`:carrier,trackingUrl,origin:get(r,"VSL")||"Korea / Asia",destination,trackingSummary:trackingBasis,status:status(`${get(r,"NOTES")} ${get(r,"RESERVED")} ${get(r,"DELIVERY EXPECTED")}`)}});inbound.push(...parcelInbound(tables[0],inbound.length+1));outbound=[];
tr.filter(useful).forEach(r=>out("Transfers",r,r=>({shipDate:get(r,"PU"),customer:get(r,"TO"),invoice:get(r,"INVOICE"),origin:get(r,"VENDOR/SUPPLIER/ORIGIN","VENDOR / SUPPLIER / ORIGIN","VENDOR","SUPPLIER","ORIGIN"),carrier:get(r,"TRUCKING"),pro:get(r,"BOL#"),units:get(r,"PLT")?`${get(r,"PLT")} Pallets`:"",destination:get(r,"TO"),rate:money(get(r,"RATE")),status:status(`${get(r,"STATUS")} ${get(r,"NOTE")} ${get(r,"INVOICE")}`)})));
ul.filter(useful).forEach(r=>out("Ulta",r,r=>({shipDate:get(r,"SHIP DATE","DATE"),customer:get(r,"DC")||"Ulta",invoice:get(r,"PO#","INVOICE"),carrier:get(r,"TRUCKING"),pro:get(r,"PRO#"),units:get(r,"TOTAL CARTONS")?`${get(r,"TOTAL CARTONS")} Cartons`:"",destination:get(r,"SHIP TO"),rate:money(get(r,"RATE")),status:get(r,"PRO#")?"Completed":status(`${get(r,"STATUS")} ${get(r,"NOTE")} ${get(r,"REMARKS")}`)})));
ih.filter(useful).forEach(r=>out("iHerb",r,r=>({shipDate:get(r,"PU","DELIVERY APPT"),customer:"iHerb",invoice:get(r,"PO#"),carrier:get(r,"TRUCKING"),pro:get(r,"BOL"),units:get(r,"QTY")?`${get(r,"QTY")} Pallets`:"",destination:get(r,"TO"),rate:money(get(r,"RATE")),status:status(`${get(r,"STATUS")} ${get(r,"NOTE")} ${get(r,"REMARKS")}`)})));
b2.filter(r=>["INVOICE","PU","TRUCKING","PRO#","PLT","QTY","RATE"].some(name=>get(r,name))&&!siteExclusions.has(`B2B/E-COM TRUCKING|${get(r,"PRO#")}`.toUpperCase())).forEach(r=>out("B2B/E-com Trucking",r,r=>({shipDate:get(r,"PU"),customer:get(r,"NOTE"),invoice:get(r,"INVOICE"),carrier:get(r,"TRUCKING"),pro:get(r,"PRO#"),units:get(r,"PLT")?`${get(r,"PLT")} Pallets`:"",destination:get(r,"TO"),rate:money(get(r,"RATE")),status:status(Object.values(r).join(" "))})));
wh.filter(r=>!/PLEASE LIST THE INVOICE WITH SHIPPING CHARGE/i.test(get(r,"CUSTOMER"))&&(get(r,"CUSTOMER")||get(r,"INVOICE NO."))&&["CUSTOMER","INVOICE NO.","SHIP DATE","PALLET TYPE","CARRIER","PRO#"].some(name=>get(r,name))).forEach(r=>out("WH Trucking Request",r,r=>({shipDate:get(r,"SHIP DATE"),customer:get(r,"CUSTOMER"),invoice:get(r,"INVOICE NO."),carrier:get(r,"CARRIER"),pro:get(r,"PRO#"),units:get(r,"PALLET TYPE")?"Pallets":"",destination:get(r,"ADDRESS"),rate:money(get(r,"RATE")),status:status(`${get(r,"STATUS")} ${get(r,"NOTE")} ${get(r,"REMARKS")}`)})));
national.filter(useful).forEach(r=>out("National Order Progress",r,r=>({shipDate:get(r,"PICK-UP DATE","START SHIP"),customer:get(r,"CHANNEL"),invoice:get(r,"PO#","ORDER#"),origin:get(r,"DEPARTMENT"),carrier:get(r,"SHIPMENT TYPE"),pro:"",units:get(r,"MEMO"),destination:"",rate:0,status:status(`${get(r,"OVERALL PO STATUS")} ${get(r,"MEMO")}`)})));
const now=new Date(),today=new Date(now.getFullYear(),now.getMonth(),now.getDate()),dated=outbound.map(r=>({r,d:date(r.shipDate)})).filter(x=>x.d&&x.d<=today);costSummary={ytd:dated.filter(x=>x.d.getFullYear()===now.getFullYear()).reduce((s,x)=>s+x.r.rate,0),mtd:dated.filter(x=>x.d.getFullYear()===now.getFullYear()&&x.d.getMonth()===now.getMonth()).reduce((s,x)=>s+x.r.rate,0),finished:outbound.filter(r=>done.has(r.status)).length};outbound=outbound.filter(r=>!finished.has(r.status));
populate();render();$("sync").textContent=unavailable.length?`${8-unavailable.length} sources imported · unavailable: ${unavailable.join(", ")}`:"All eight outbound/inbound sources imported";$("dot").classList.toggle("sync-error",unavailable.length>0);$("updated").textContent=new Date().toLocaleString();}catch(e){$("dot").classList.add("sync-error");$("sync").textContent=`Workbook sync issue: ${e.message}`;console.error(e)}}
function date(v){const m=String(v).match(/(\d{1,2})\/(\d{1,2})(?:\/(\d{2,4}))?/);if(!m)return null;const mo=+m[1];let y=m[3]?+m[3]:mo>=9?2025:2026;if(y<100)y+=2000;return new Date(y,mo-1,+m[2])}function soon(v){const d=date(v);if(!d)return false;const a=new Date();a.setHours(0,0,0,0);return d>=a&&d<=new Date(+a+14*864e5)}
function uniqueParts(values){return[...new Set(values.flatMap(v=>String(v||"").split(/[\r\n,;]+/)).map(v=>v.trim()).filter(Boolean))]}
function dateRange(values){const sorted=values.map(v=>({v,d:date(v)})).filter(x=>x.d).sort((a,b)=>a.d-b.d);if(!sorted.length)return uniqueParts(values).join(" · ");const first=sorted[0].v,last=sorted.at(-1).v;return first===last?first:`${first} – ${last}`}
function numericText(values){const present=values.map(v=>String(v||"").trim()).filter(Boolean),nums=present.map(v=>v.match(/^\d+(?:\.\d+)?$/)?.[0]);if(present.length&&nums.every(Boolean)){const sum=nums.reduce((s,n)=>s+Number(n),0);return Number.isInteger(sum)?String(sum):sum.toFixed(2)}return uniqueParts(present).join(" · ")}
function primarySource(v){return String(v||"").split(" · ")[0]||String(v||"")}
function sourceTags(v){return uniqueParts(String(v||"").split(" · ")).map(s=>`<span class="source-tag source-${sourceKey(s)}">${esc(s)}</span>`).join("")}
function consolidateNearbyOutboundRows(rows){const groups=new Map,withoutCustomer=[];rows.forEach(r=>{const customer=String(r.customer||"").trim().toUpperCase().replace(/\s+/g," ");if(!customer){withoutCustomer.push(r);return}groups.set(customer,[...(groups.get(customer)||[]),r])});const merged=[...groups.values()].flatMap(customerRows=>{const sorted=[...customerRows].sort((a,b)=>(date(a.shipDate)?.getTime()||0)-(date(b.shipDate)?.getTime()||0)),clusters=[];sorted.forEach(r=>{const cluster=clusters.at(-1),first=cluster?.[0],within=cluster&&date(r.shipDate)&&date(first.shipDate)&&date(r.shipDate)-date(first.shipDate)<=3*864e5;if(within)cluster.push(r);else clusters.push([r])});return clusters.map(cluster=>{if(cluster.length===1)return cluster[0];const base=cluster[0];return{...base,source:uniqueParts(cluster.map(r=>r.source)).join(" · "),shipDate:dateRange(cluster.map(r=>r.shipDate)),invoice:uniqueParts(cluster.map(r=>r.invoice)).join(" · "),origin:uniqueParts(cluster.map(r=>r.origin)).join(" · "),pro:uniqueParts(cluster.map(r=>r.pro)).join(" · "),carrier:uniqueParts(cluster.map(r=>r.carrier)).join(" · "),units:uniqueParts(cluster.map(r=>r.units)).join(" · "),qty:numericText(cluster.map(r=>r.qty)),length:uniqueParts(cluster.map(r=>r.length)).join(" · "),width:uniqueParts(cluster.map(r=>r.width)).join(" · "),height:uniqueParts(cluster.map(r=>r.height)).join(" · "),freightClass:uniqueParts(cluster.map(r=>r.freightClass)).join(" · "),subClass:uniqueParts(cluster.map(r=>r.subClass)).join(" · "),destination:uniqueParts(cluster.map(r=>r.destination)).join(" · "),rate:cluster.reduce((sum,r)=>sum+r.rate,0),status:cluster.some(r=>r.status==="Shipping")?"Shipping":base.status}})});return[...withoutCustomer,...merged].sort((a,b)=>(date(a.shipDate)?.getTime()??Number.MAX_SAFE_INTEGER)-(date(b.shipDate)?.getTime()??Number.MAX_SAFE_INTEGER))}
function populate(){const fill=(id,vals)=>{const s=$(id),first=s.firstElementChild;s.innerHTML="";s.append(first);[...new Set(vals.filter(Boolean))].sort().forEach(v=>s.insertAdjacentHTML("beforeend",`<option>${esc(v)}</option>`))};fill("source",outbound.map(r=>r.source));fill("outStatus",outbound.map(r=>r.status))}
function render(){const activeOutbound=outbound.filter(r=>!finished.has(r.status)).length,active=inbound.filter(r=>!finished.has(r.status)).length,cost=outbound.filter(r=>!finished.has(r.status)).reduce((a,r)=>a+r.rate,0);$("metrics").innerHTML=[["Active outbound shipments",activeOutbound,"Completed, cancelled and grey rows excluded"],["Delivered / completed",costSummary.finished,"All finished outbound source rows"],["Inbound active",active,"Ocean + air + parcel"],["Inbound ocean",inbound.filter(r=>r.mode==="Ocean").length,"Container shipments"],["Small parcel",inbound.filter(r=>r.mode==="Small Parcel").length,"UPS / FedEx / DHL + more"],["Scheduled outbound cost",`$${cost.toLocaleString()}`,"Active numeric charge rows"],["Year-to-date outbound cost",`$${costSummary.ytd.toLocaleString()}`,"Invoice or quoted charges through today"],["Month-to-date outbound cost",`$${costSummary.mtd.toLocaleString()}`,"Invoice or quoted charges this month"]].map((m,i)=>`<article class="metric-card ${i===5?"accent-card":i===6?"ytd-card":i===7?"mtd-card":""}"><span>${m[0]}</span><strong>${m[1]}</strong><small>${m[2]}</small></article>`).join("");upcoming("inboundUpcoming",inbound.filter(r=>!finished.has(r.status)&&soon(r.eta)),"in");upcoming("outboundUpcoming",consolidateNearbyOutboundRows(outbound.filter(r=>!finished.has(r.status)&&soon(r.shipDate))),"out");renderOut();renderIn()}
function upcoming(id,rows,type){const start=new Date();start.setHours(0,0,0,0);const days=Array.from({length:14},(_,i)=>new Date(+start+i*864e5));$(id).innerHTML=`<div class="schedule-scroll"><div class="schedule-grid">${days.map(day=>{const matches=rows.filter(r=>{const d=date(type==="in"?r.eta:r.shipDate);return d&&d.toDateString()===day.toDateString()});const heading=matches.length?(matches.some(r=>r.status==="Shipping")?"IN TRANSIT":"ESTIMATED / CHANGED"):"&nbsp;";return`<div class="schedule-day"><div class="schedule-status ${heading==="IN TRANSIT"?"status-transit":matches.length?"status-estimated":""}">${heading}</div><div class="schedule-date-label">${day.toLocaleDateString("en-US",{month:"2-digit",day:"2-digit",year:"2-digit"})}</div><div class="schedule-cell">${matches.map(r=>{const label=r.shipmentNo||r.container||r.hbl||r.mbl,detail=r.container||r.carrier,cls=type==="in"?"schedule-inbound-item":`schedule-source-${sourceKey(primarySource(r.source))}`;return type==="in"?`<div class="schedule-item ${cls}"><strong>${esc(label||"Shipment")}</strong><span>${esc(detail)}</span></div>`:`<div class="schedule-item ${cls}"><div class="schedule-outbound-fields"><span><b>Customer</b>${esc(r.customer)||"—"}</span><span><b>Invoice</b>${esc(r.invoice)||"—"}</span></div><small>${esc(r.source)}${r.pro?` · ${esc(r.pro)}`:""}</small></div>`}).join("")}</div></div>`}).join("")}</div></div>`}
function renderOut(){const q=$("outSearch").value.toLowerCase(),src=$("source").value,st=$("outStatus").value,rows=consolidateNearbyOutboundRows(outbound.filter(r=>!finished.has(r.status)&&(!q||Object.values(r).join(" ").toLowerCase().includes(q))&&(!src||r.source===src)&&(!st||r.status===st)));$("outCount").textContent=`${rows.length} rows`;$("outRows").innerHTML=rows.map(r=>`<tr class="row-source-${sourceKey(primarySource(r.source))}"><td><div class="source-cell">${sourceTags(r.source)}</div></td><td class="date-cell">${esc(r.shipDate)}</td><td><strong>${esc(r.customer)}</strong><small>${esc(r.invoice)}</small></td><td>${esc(r.origin)||"—"}</td><td><strong>${esc(r.carrier)}</strong><small>${esc(r.pro)}</small></td><td>${esc(r.units)}</td><td>${esc(r.qty)||"—"}</td><td>${esc(r.length)||"—"}</td><td>${esc(r.width)||"—"}</td><td>${esc(r.height)||"—"}</td><td>${esc(r.freightClass)||"—"}</td><td>${esc(r.subClass)||"—"}</td><td>${esc(r.destination)}</td><td class="money">$${r.rate.toLocaleString()}</td><td><span class="status-select status-${r.status.toLowerCase()}">${r.status}</span></td></tr>`).join("")}
function renderIn(){const q=$("inSearch").value.toLowerCase(),mode=$("mode").value,rows=inbound.filter(r=>!finished.has(r.status)&&(!q||Object.values(r).join(" ").toLowerCase().includes(q))&&(!mode||r.mode===mode)).sort((a,b)=>(date(a.eta)?.getTime()??Number.MAX_SAFE_INTEGER)-(date(b.eta)?.getTime()??Number.MAX_SAFE_INTEGER)||String(a.shipmentNo||a.container||"").localeCompare(String(b.shipmentNo||b.container||"")));$("inCount").textContent=`${rows.length} rows`;$("inRows").innerHTML=rows.map(r=>`<tr><td><span class="mode-tag mode-${r.mode.toLowerCase()}">${r.mode}</span></td><td class="date-cell">${esc(r.eta)}</td><td><strong>${esc(r.shipmentNo)}</strong><small>Imports</small></td><td>${esc(r.mbl)||"—"}</td><td>${esc(r.hbl)||"—"}</td><td>${r.container?`<a class="container-link" target="_blank" rel="noreferrer" href="${esc(r.trackingUrl)}">${esc(r.container)} →</a>`:"—"}</td><td>${esc(r.carrier)}</td><td><strong>${esc(r.origin)}</strong><small>${esc(r.trackingSummary||`to ${r.destination}`)}</small></td><td><span class="status-select status-${r.status.toLowerCase()}">${r.status}</span></td></tr>`).join("")}

async function load(){await Promise.allSettled([loadBase(),loadImportSchedule()]);updateTasksFromShipments();renderTasks();scheduleAutoRefresh()}
async function loadImportSchedule(){try{const rows=objects(await sheet("INBOUND SHIPMENTS DATA","U238:AI260"));importScheduleRows=rows.filter(useful);renderImportSchedule()}catch(error){importScheduleRows=[];const target=$("importScheduleTimeline");if(target)target.innerHTML=`<p class="empty-state">Import schedule unavailable: ${esc(error.message)}</p>`;console.error("Import schedule sync failed",error)}}
function taskStatus(value){const normalized=String(value||"").toLowerCase();if(/delay|hold|fda|fws/.test(normalized))return"blocked";if(/shipping|transit|progress/.test(normalized))return"in_progress";if(/deliver|receive|complete|cancel/.test(normalized))return"completed";return"pending"}
function updateTasksFromShipments(){taskRows=[...outbound.map(row=>({id:`OUT-${row.invoice||row.pro||row.id}`,type:"Outbound shipment",customer:row.customer||"Unknown",status:taskStatus(row.status),dueDate:row.shipDate,reference:row.invoice||row.pro||"—",description:`${row.customer||"Unknown"} ${row.invoice||row.pro||""}`.trim()})),...inbound.map(row=>({id:`IN-${row.shipmentNo||row.container||row.hbl||row.mbl||row.id}`,type:row.mode||"Inbound shipment",customer:row.shipmentNo||row.container||"Unknown",status:taskStatus(row.status),dueDate:row.eta,reference:row.container||row.hbl||row.mbl||"—",description:`${row.shipmentNo||row.container||"Unknown"} ${row.carrier||""}`.trim()}))];saveTasksToStorage()}
function loadTasksFromStorage(){try{const value=localStorage.getItem("dashboard_tasks");taskRows=value?JSON.parse(value):[]}catch(error){taskRows=[];console.error("Task storage unavailable",error)}}
function saveTasksToStorage(){try{localStorage.setItem("dashboard_tasks",JSON.stringify(taskRows))}catch(error){console.error("Could not persist tasks",error)}}
function renderTasks(){const table=$("taskTable");if(!table)return;const query=$("taskSearch").value.toLowerCase(),statusFilter=$("taskStatusFilter").value,rows=taskRows.filter(task=>(!query||`${task.id} ${task.type} ${task.customer} ${task.description}`.toLowerCase().includes(query))&&(!statusFilter||task.status===statusFilter));$("taskCount").textContent=`${rows.length} tasks`;$("taskRows").innerHTML=rows.slice(0,1000).map(task=>`<tr><td><strong>${esc(task.id)}</strong><small>${esc(task.type)}</small></td><td>${esc(task.customer)}</td><td><span class="status-select task-status-${task.status}">${esc(task.status.replace("_"," "))}</span></td><td>${esc(task.dueDate||"—")}</td><td>${esc(task.reference||"—")}</td></tr>`).join("")||`<tr><td colspan="5" class="empty-state">No configured tasks match the current filters.</td></tr>`}
function renderImportSchedule(){const target=$("importScheduleTimeline");if(!target)return;const first=importScheduleRows[0]||{},days=Object.keys(first).filter(key=>key&&!/^COL_\d+$/i.test(key)&&key!=="SCHEDULE").slice(0,14),items=days.map(day=>({day,values:importScheduleRows.flatMap(row=>String(row[day]||"").split(/\n+/).map(value=>value.trim()).filter(Boolean))}));$("importScheduleCount").textContent=`${items.reduce((total,day)=>total+day.values.length,0)} imports`;target.innerHTML=items.map(day=>`<article class="import-day"><strong>${esc(day.day)}</strong>${day.values.length?day.values.slice(0,8).map(value=>`<span>${esc(value)}</span>`).join(""):"<em>No imports</em>"}</article>`).join("")||`<p class="empty-state">No import schedule rows available.</p>`}
function clearAutoRefresh(){if(autoRefreshTimer){clearTimeout(autoRefreshTimer);autoRefreshTimer=null}if(countdownTimer){clearInterval(countdownTimer);countdownTimer=null}}
function scheduleAutoRefresh(){clearAutoRefresh();if(!autoRefreshEnabled){$("nextRefresh").textContent="Auto-refresh off";return}nextRefreshAt=Date.now()+AUTO_REFRESH_INTERVAL;updateRefreshCountdown();countdownTimer=setInterval(updateRefreshCountdown,1000);autoRefreshTimer=setTimeout(load,AUTO_REFRESH_INTERVAL)}
function updateRefreshCountdown(){const target=$("nextRefresh");if(!target||!nextRefreshAt)return;const remaining=Math.max(0,nextRefreshAt-Date.now()),minutes=Math.floor(remaining/60000),seconds=Math.floor(remaining%60000/1000);target.textContent=remaining?`Next refresh: ${minutes}:${String(seconds).padStart(2,"0")}`:"Refreshing…"}
["outSearch","source","outStatus"].forEach(id=>$(id).addEventListener("input",renderOut));["inSearch","mode"].forEach(id=>$(id).addEventListener("input",renderIn));["taskSearch","taskStatusFilter"].forEach(id=>$(id).addEventListener("input",renderTasks));$("refresh").addEventListener("click",load);$("autoRefresh").addEventListener("change",event=>{autoRefreshEnabled=event.target.checked;scheduleAutoRefresh()});loadTasksFromStorage();load();
